# AgniNetra

National Thermal Intelligence Platform.

## Run the backend

From the `AgniNetra` directory:

```powershell
venv\Scripts\activate
uvicorn backend.main:app --reload
```

Open `http://127.0.0.1:8000/docs` for the interactive API documentation.